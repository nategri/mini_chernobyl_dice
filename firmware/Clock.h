#ifndef CLOCK_H
#define CLOCK_H

#include "pins.h"
#include "display.h"

/*
class Clock {
  private:
    LedScreen* ledScreen;
    SoftDS1307RTC* rtc;
  public:
    Clock(LedScreen*, SoftDS1307RTC*);
    void show();
    void handleUpdate();
}
*/

#endif
